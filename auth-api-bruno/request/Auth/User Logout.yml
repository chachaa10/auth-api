info:
  name: User Logout
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/auth/logout"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
