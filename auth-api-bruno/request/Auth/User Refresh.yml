info:
  name: User Refresh
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/auth/refresh"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
